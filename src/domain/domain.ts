export type Transaction = {
    id: number;
    value: number;
    dateTime: Date; 
};

export type Wallet = {
    id: number;
    value: number;   
}

export type User = {
    id: number;
    name: string;
}